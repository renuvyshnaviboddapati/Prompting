DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_db_name
DB_USER=your_db_user
DB_PASSWORD=your_db_password
# Optional: full connection URL (uncomment and set if preferred)
# DATABASE_URL=postgresql://user:password@localhost:5432/your_db_name

# API keys and confidentials (placeholders). Keep this file out of source control.
# Replace the values below with real secrets or use a secrets manager.
API_KEY=your_api_key
SECRET_KEY=your_secret_key
OAUTH_CLIENT_ID=your_oauth_client_id
OAUTH_CLIENT_SECRET=your_oauth_client_secret
ANOTHER_SECRET=your_other_secret

